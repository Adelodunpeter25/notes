import { LoginCard } from "@/components/auth";

export function LoginPage() {
  return (
    <main className="flex h-[calc(100vh-48px)] items-center justify-center bg-background p-6">
      <LoginCard />
    </main>
  );
}
